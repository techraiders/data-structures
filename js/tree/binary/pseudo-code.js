// Constructor
  // value
  // left
  // right

// Methods
	// insert (value) goal: find its proper place
			// if value < current
			// if (left)	go left
			// else insert
	// if value > current
			// if (right) go right
			// else insert
	// contains